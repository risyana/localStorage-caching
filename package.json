{
  "name": "localstorage-cache",
  "version": "1.0.0",
  "description": "caching with localStorage",
  "main": "index.js",
  "scripts": {
    "start": "reload -b",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "risyana@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "reload": "^2.3.1"
  }
}
